from setuptools import setup

setup(
   name='metaopt',
   version='1.0',
   description='A useful module',
   author='Daniel Jiwoong Im',
   author_email='ji641@nyu.edu',
   packages=['metaopt'],  #same as name
   #install_requires=['bar', 'greek'], #external packages as dependencies
)

